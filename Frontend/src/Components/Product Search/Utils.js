import * as Scroll from "react-scroll";

const TodaysDeal="Today's Deal";
const Available="Out of Stock";
const StarRate="Rating";
const scroll=Scroll.animateScroll;

const defaultPrice=0;
const defaultRating=0;
const defaultDeal=false;
const defaultStock=false;
const defaultPage=1;
const defaultPageSize=6;

export {
    StarRate,TodaysDeal,Available,scroll,defaultPrice,defaultRating,defaultDeal,defaultStock,defaultPage,defaultPageSize
}